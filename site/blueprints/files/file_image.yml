title: Image entity

accept: image/*

num: zero

options:
  changeName: true
  delete: true
  replace: true
  update: true

tabs:

  tab_info:
    extends: tabs/info
    columns:
      column_left:
        sections:

          info_section_fields2: false
                
          info_section_uploads: false

      column_right:
        sections:
          info_section_fields5:
            type: fields
            fields:

              date: fields/date_historical

              location: fields/location/location

              starring:
                type: tags
                label: Starring
                help: People, Objects, Buildings, ...
                options:
                  type: query
                  query: site.archive.entities
                  text: '{{ page.title }}'
                  value: '{{ page.id }}'

  tab_contexts: tabs/contexts
  
  tab_meta:
    extends: tabs/meta
    columns:
      meta_column_top:
        fields:
          signature:
            text: |
              **{{ file.id }}**
              
              Kirbytag: &#40;link: {{ file.id }})
              Permanent URL: {{ file.permalink }}

      meta_column_left:
        sections:
          meta_section_fields1:
            fields:

              category:
                options:
                  query: file.parent.type_categories.toStructure

              credits:
                label: Credits for creating this file
                fields:
                  title:
                    options:
                      query: file.parent.credit_roles.toStructure

              copyright: fields/files/copyright

              showDownloadButton: fields/files/showDownloadButton

              is_360:
                label: Panorama photo
                type: toggle
                help: Panoramas will be cropped to 4096 x 2048 pixels
                text:
                    - Normal
                    - 'This is a 360° panorama'