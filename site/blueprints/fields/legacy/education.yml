type: structure
label: Education (Legacy)
fields:

  date_start:
    extends: fields/date_historical
    label: Date

  text:
    type: textarea
    buttons: false

  location:
    extends: fields/location/legacy/location_city
    multiple: false