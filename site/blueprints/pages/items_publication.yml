title: Pool of Publications
icon: share

num: zero

options:
  # valid for all users
  preview: true
  read: true
  delete: false
  duplicate: false
  changeTemplate: false
  update: true
  changeStatus: false
  # only allow some users
  changeSlug:
    *: false
    admin: true
  changeTitle:
    *: false
    admin: true

extends: pages/items

tabs:

  tab_archive:
    columns:
      column_left:
        sections:
          section_pool:
            templates:
              - item_publication
              - item_event

      column_right:
        sections:
          section_fields:
            fields:
              highlights:
                max: 16
