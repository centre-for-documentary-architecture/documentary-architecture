title: Pool of Audios
icon: file-audio

num: zero

options:
  # valid for all users
  preview: true
  read: true
  # only allow some users
  changeTemplate: false
  update: true
  changeSlug: false
  changeStatus: false
  changeTitle: false
  delete: false
  duplicate: false

extends: pages/files

tabs:

  tab_archive:
    columns:
      column_left:
        sections:
          section_pool:
            image:
              back: '#f9f9f9'
            templates:
              - file_audio

      column_right:
        sections:
          
          section_info1:
            type: info
            headline: About uploading audio files
            text: | 
              Audio files should be provided as **Mp3** preferable with
              • 44.1 sample rate
              • 160 or 192 kbps bitrate with joint stereo