title: Pool of Audios
icon: file-audio

num: zero

options:
  # valid for all users
  preview: true
  read: true
  # only allow some users
  changeTemplate: false
  update: true
  changeSlug: false
  changeStatus: false
  changeTitle: false
  delete: false
  duplicate: false

extends: pages/files

tabs:

  tab_archive:
    columns:
      column_left:
        sections:
          section_pool:
            image:
              back: '#f9f9f9'
            templates:
              - file_audio