title: Video File
icon: file-video

num: zero

extends: pages/file

tabs:
  tab_info:
    columns:
      column_left:
        sections:
          info_section_fields2:
            fields:

              thumbnail:
                image:
                  ratio: 16/9
                help: Provide Thumbnail in best quality and same aspect ratio as video (16:9)

              remote_file: fields/files/remove_video

          wiki: sections/service/wiki

      column_right:
        sections:
          info_section_fields5:
            fields:
              location_start:
                multiple: true

  tab_meta:
    columns:
      meta_column_left:
        sections:
          meta_section_fields1:
            fields:

              duration: fields/files/duration
