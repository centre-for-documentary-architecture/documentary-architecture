title: Archive of other items
icon: draft

num: zero

options:
  # valid for all users
  preview: true
  read: true
  duplicate: false
  changeTemplate: false
  update: true
  changeStatus: true
  # only allow some users
  changeSlug:
    *: false
    admin: true
  changeTitle:
    *: false
    admin: true
  delete:
    *: false
    admin: true

tabs:

  tab_archive:
    label: Archive
    icon: table
    sections:

      section_pool:
        extends: sections/pages_pool
        headline: Items
        templates:
          - item

  tab_settings: tabs/settings