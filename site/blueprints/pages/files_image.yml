title: Pool of Images
icon: file-image

num: zero

options:
  # valid for all users
  preview: true
  read: true
  # only allow some users
  changeTemplate: false
  update: true
  changeSlug: false
  changeStatus: false
  changeTitle: false
  delete: false
  duplicate: false

extends: pages/files

tabs:

  tab_archive:
    columns:
      column_left:
        sections:
          section_pool: sections/files_image