title: Locations
icon: globe

num: zero

options:
  changeStatus: false
  create: false
  duplicate: false
  delete: false

columns:

  - width: 1/2
    sections:

      locations:
        extends: sections/pages
        template: location
        limit: 50
        info: "{{ page.location.toObject.country }}"