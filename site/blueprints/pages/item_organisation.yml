title: Organisation
icon: users

num: zero

extends: pages/item

tabs:
  tab_info:
    columns:
      column_right:
        sections:
          info_section_fields5:
            fields:

              location: fields/location/location

              members:
                type: tags
                label: Members
                options:
                  type: query
                  query: site.archive('persons').children
                  text: '{{ page.title }}'
                  value: '{{ page.id }}'