title: Object
icon: box

num: zero

extends: pages/item

tabs:
  tab_info:
    columns:

      column_right:
        sections:
          info_section_fields5:
            fields:

              location: fields/location/location

              material:
                label: Material
                type: tags
                options:
                  type: query
                  query: site.archive('materials').children
                  text: '{{ page.title }}'
                  value: '{{ page.id }}'

              manufacturer:
                label: Manufacturer
                type: tags
                options:
                  type: query
                  query: site.archive('organisations').children
                  text: '{{ page.title }}'
                  value: '{{ page.id }}'