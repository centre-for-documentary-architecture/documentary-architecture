title: Event
icon: calendar

num: zero

extends: pages/item

tabs:
  tab_info:
    columns:
      column_right:
        sections:
          info_section_fields5:
            fields:

              timeline_new:
                extends: fields/location/timeline

              location_start:
                extends: fields/location/legacy/location

              timeline: fields/legacy/timeline

  tab_meta:
    columns:
      meta_column_left:
        width: 1/2
        sections:

          meta_section_fields1:
            type: fields
            fields:

              category:
                extends: fields/category

              sources: false

              research_methods: fields/research_methods

              tags: fields/tags
