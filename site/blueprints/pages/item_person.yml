title: Person
icon: user

num: zero

extends: pages/item

tabs:
  tab_info:
    columns:
      column_right:
        sections:
          info_section_fields5:
            fields:

              timeline: fields/location/timeline

              occupation:
                type: tags
                label: Occupation
                options:
                  type: query
                  query: page.siblings.pluck('occupation',',',true)

              projects:
                type: tags
                label: Projects
                options:
                  type: query
                  query: site.archive('buildings').children
                  text: '{{ page.title }}'
                  value: '{{ page.id }}'
              
              bio:
                extends: fields/body
                label: Biography
                buttons: false
                help: false