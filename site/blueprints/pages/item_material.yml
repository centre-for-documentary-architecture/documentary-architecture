title: Material
icon: grid

num: zero

extends: pages/item

tabs:
  tab_info:
    columns:
      column_right:
        sections:
          info_section_fields5:
            fields:

              date_start: false

              location_new:
                extends: fields/location/location
                label: Origin

              location_start:
                extends: fields/location/legacy/location_city