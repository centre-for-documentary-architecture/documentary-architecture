title: Pool of Videos
icon: file-video

num: zero

options:
  # valid for all users
  preview: true
  read: true
  # only allow some users
  changeTemplate: false
  update: true
  changeSlug: false
  changeStatus: false
  changeTitle: false
  delete: false
  duplicate: false

extends: pages/files

tabs:

  tab_archive:
    columns:
      column_left:
        sections:
          section_pool:
            image:
              cover: true
            templates:
              - file_video