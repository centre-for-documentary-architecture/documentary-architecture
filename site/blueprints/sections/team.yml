type: fields
fields:

  info:
    type: info
    label: Kirbytag
    theme: white
    text: "(&af;user: {{ user.name }})"

  show_email:
    type: toggle
    label: Show email address
    icon: email
    text:
      - 'No'
      - 'Yes'

  links:
    extends: fields/new/urls

  bio:
    extends: fields/new/writer

  
